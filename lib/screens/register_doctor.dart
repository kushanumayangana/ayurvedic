import 'package:flutter/material.dart';

class RegisterDoctor extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Doctor Registration")),
      body: Center(child: Text("Register as Doctor")),
    );
  }
}
