import 'package:flutter/material.dart';

class RegisterSeller extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Seller Registration")),
      body: Center(child: Text("Register as Seller")),
    );
  }
}
